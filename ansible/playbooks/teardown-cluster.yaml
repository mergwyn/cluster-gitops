# playbooks/teardown-cluster.yaml
---
- name: Teardown k3s cluster
  hosts: k3s_cluster
  gather_facts: no
  vars:
    operation: "teardown"
    remove_profiles: true
    remove_kubeconfig: true
  roles:
    - role: k3s_cluster