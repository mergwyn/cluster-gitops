---
name: ApplicationSet Safety

on:
  pull_request:
    paths:
      - argocd/appsets/**
      - "**/app.yaml"

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6

      - name: Install yq
        uses: mikefarah/yq@065b200af9851db0d5132f50bc10b1406ea5c0a8 # v4.50.1

      - name: Validate required fields in app.yaml
        run: |
          set -e
          echo "🔍 Validating required keys in app.yaml files"
          find . -name app.yaml -print0 | while IFS= read -r -d '' f; do
            yq e '
              .name
              and .namespace
              and .wave
              and .path
            ' "$f" > /dev/null \
              || { echo "❌ Missing required field in $f"; exit 1; }
          done

      - name: Validate app.yaml path exists
        run: |
          set -e
          echo "📂 Validating path references"
          find . -name app.yaml -print0 | while IFS= read -r -d '' f; do
            PATH_VAL=$(yq e '.path' "$f")
            if [[ ! -d "$PATH_VAL" ]]; then
              echo "❌ Path does not exist: $PATH_VAL (from $f)"
              exit 1
            fi
          done

      - name: Detect duplicate app names
        run: |
          set -e
          echo "🔁 Checking for duplicate app names"
          DUPES=$(
            find . -name app.yaml -exec yq e '.name' {} \; \
              | sort | uniq -d
          )
          if [[ -n "$DUPES" ]]; then
            echo "❌ Duplicate application names found:"

