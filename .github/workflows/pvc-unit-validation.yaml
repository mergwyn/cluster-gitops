---
name: PVC Unit Validation

on:
  pull_request:
    paths:
      - '**.yaml'
      - '**.yml'

jobs:
  validate-units:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7

      - name: Ensure PVCs use binary units (Gi/Mi)
        run: |
          # Look for storage: lines that end in a number + G (but not Gi)
          # Examples of failures: "10G", "5G"
          # Examples of passes: "10Gi", "500Mi"
          ERRORS=$(grep -rE "(storage|size):.*[0-9]+(G|M)($|\s)" . --exclude-dir=.git || true)

          if [ ! -z "$ERRORS" ]; then
            echo "::error::Found PVC storage requests using decimal units (G/M). Use binary units (Gi/Mi) to prevent 'field can not be less than status.capacity' errors."
            echo "$ERRORS"
            exit 1
          fi
          echo "All storage units look good!"
