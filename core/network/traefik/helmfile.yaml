---
bases:
  - ../../../bases/defaults.yaml
  - ../../../bases/repositories.yaml

apiVersions:
  - monitoring.coreos.com/v1

releases:
  - name: traefik
    chart: traefik/traefik
    version: 37.1.2
    values:
      - values.yaml
