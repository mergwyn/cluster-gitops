---
bases:
  - ../../../clusters/environments.yaml

repositories:
  - name: generic-device-plugin
    url: https://charts.gabe565.com

releases:
  - name: generic-device-plugin
    chart: generic-device-plugin/generic-device-plugin
    version: 0.1.3
    namespace: '{{ env "HELMFILE_NAMESPACE" }}'
    values:
      - values.yaml
