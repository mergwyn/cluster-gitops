---
bases:
  - ../../../bases/defaults.yaml
  - ../../../bases/repositories.yaml

apiVersions:
  - monitoring.coreos.com/v1

releases:
  - name: argocd
    chart: argoproj/argo-cd
    version: 8.5.8
    values:
      - values.yaml

  - name: argocd-externalsecret
    chart: bedag/raw
    version: 2.0.2
    values:
      - externalsecret.yaml
