kube-vip:
  config:
    address: 10.58.0.48
    #address: {{ tpl .CLUSTER_EXTERNAL_SUBNET_PREFIX  }}.{{ tpl .CLUSTER_LB }}

  env:
    cp_enable: "true"
    vip_arp: "true"
    vip_leaderelection: "true"
